fn main() {
    eprintln!("nyet — read-only database access for AI agents.");
    eprintln!("Your AI agent can look. For everything else — nyet.");
    eprintln!();
    eprintln!("This is an early placeholder release; the tool is under development.");
    eprintln!("Roadmap and design: https://github.com/stasmarkin/nyetdb");
    std::process::exit(1);
}
